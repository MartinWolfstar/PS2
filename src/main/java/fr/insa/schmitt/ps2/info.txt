dans le fichier  Dessin canvas:


dans le fichier groupe:

il peut y avoir un probleme si le groupe est vide dans la fonction NoeudPlusProche



reste à faire:

mettre des try/catch pour eviter les erreurs (surtout si groupe est vide)

oter zc

noeud appui direction force

parcourir contient barre et comparer a contient
si c'est le meme objet alors l'onjet de contient = l'objet de contient barre

pareil pour noeud







